
    <video autoplay muted loop style="position: fixed; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; z-index: -1;">
    <source src="/media/beach.mp4" type="video/mp4">
    </video>

    <div class="container" style="position: relative; z-index: 1; display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh; margin-top: -50px;">
        <!-- Content goes here -->
        <h1 style="text-align: center; font-size: 8em; margin-bottom: -5px;"><span class="orange-text text-lighten-3">Bucket</span></h1>
        <h4 style="text-align: center; font-size: 1.5em; font-weight: bold;"><span class="grey-text text-darken-4">Life is short, so your bucket list shouldn't be. It's time you started doing the things you've set aside for later because later isn't guaranteed. Get started with Bucket and make every moment count.</span></h4>
        <div style="text-align: center;">
            {{#if logged_in}}
            <h5>Welcome back {{name}}</h5>
            <a href="/profile" class="waves-effect waves-light btn">Continue to Bucket</a>
            <button id="logOutButton" class="waves-effect waves-light btn">Log out</button>
            {{else}}
            <a href="/login" class="waves-effect waves-light btn grey darken-4"><span class="orange-text text-lighten-3">Get Started</span></a>
            {{/if}}
        </div>

        {{#if logged_in}}
        <script src="/js/logout.js"></script>
        {{/if}}
    </div>

